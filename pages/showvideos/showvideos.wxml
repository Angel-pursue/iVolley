<view class="container">
  <view class="video-list">
    <view class="video-item" wx:for="{{videos}}" wx:key="id" bindtap="playVideo">
      <image src="{{item.cover}}" class="video-cover"></image>
      <text class="video-title">{{item.title}}</text>
    </view>
  </view>
</view>
